# Configuração do Servidor
PORT=3001

# Configuração do Banco de Dados (PostgreSQL)
DB_USER=postgres
DB_HOST=db
DB_NAME=gestao_frete
DB_PASSWORD=postgres
DB_PORT=5432

# Configuração de Autenticação (JWT)
# IMPORTANTE: Altere estas chaves secretas em produção!
JWT_SECRET=sua_chave_secreta_super_segura_aqui
JWT_REFRESH_SECRET=sua_chave_refresh_super_segura_aqui

# Observações:
# - Copie este arquivo para .env e ajuste os valores conforme seu ambiente
# - Nunca comite o arquivo .env no git (já está no .gitignore)
# - Em produção, use chaves secretas fortes e únicas
# - Para gerar chaves secretas fortes, use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
